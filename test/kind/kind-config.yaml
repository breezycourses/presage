kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: presage-e2e
nodes:
  - role: control-plane
  # A worker so pods land somewhere other than the control plane, and so
  # scheduling is a real step rather than a formality.
  - role: worker
